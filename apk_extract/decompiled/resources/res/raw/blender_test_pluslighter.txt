inline half4 premulalpha(half4 color){
     return half4(color.rgb * color.a, color.a);
}

half4 main(half4 src, half4 dst) {
     half4 src_raw = premulalpha(src);
     half4 dst_raw = premulalpha(dst);
     half4 r = min(half4(1.0),  dst_raw + src_raw);
     return r;
}

